#include <stdio.h>

typedef struct node{
    int n;
    struct node *next;
}Node;

void insert(Node n){
    Node *other = (Node *) malloc (sizeof(Node));
    while (n.next){
        
    }
}

int main () {
    Node *n = (Node *) malloc (sizeof(Node));
}